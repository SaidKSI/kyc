# Backend API URL — FastAPI runs on port 8000 in dev
NEXT_PUBLIC_API_URL=http://localhost:8000

# Node environment — controls dev-only features (e.g. file upload fallback in capture flow)
NODE_ENV=development
